# API Lie Detector

Checks if API endpoints actually exist and return what their documentation promises. Because API docs lie more than a toddler with cookie crumbs on their face. Run: `python api_lie_detector.py <url> [expected_status]`